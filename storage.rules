rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // Anyone can view product images; only a signed-in admin can upload or delete them.
    match /products/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
